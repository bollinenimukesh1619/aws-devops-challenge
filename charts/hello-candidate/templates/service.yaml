apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-candidate.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "hello-candidate.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "hello-candidate.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http

